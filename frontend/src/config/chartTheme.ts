// Shared Recharts theme — amber-led PropVista palette. Keep chart colors here
// so Dashboard/Analytics (and any future chart) stay visually consistent.

/** Primary series color (lines, single-series bars). */
export const CHART_PRIMARY = "#f59e0b";
/** Soft horizontal gridlines. */
export const CHART_GRID = "#f2f4f7";
/** Axis ticks / labels (muted). */
export const CHART_AXIS = "#8a93a2";
/** Category series (donuts, multi-series): amber, blue, green, purple, red, sky. */
export const CHART_PALETTE = ["#f59e0b", "#2563eb", "#16a34a", "#7c3aed", "#e5484d", "#0ea5e9"];
